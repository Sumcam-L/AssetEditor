<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="Firaxis.MVVMBase.MessageBoxView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" Height="400" Width="600" xmlns:mvvmbase="clr-namespace:Firaxis.MVVMBase;assembly=Firaxis.MVVMBase" Title="{Binding Title}">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="auto" />
      <RowDefinition Height="auto" />
      <RowDefinition Height="auto" />
    </Grid.RowDefinitions>
    <TextBlock Name="TitleBlock" Grid.Row="0" FontSize="20" FontWeight="Bold" TextWrapping="Wrap" Margin="10" Width="500" Text="{Binding Title}" />
    <ListBox Name="MessagesListBox" Grid.Row="1" Width="520" Height="280" ItemsSource="{Binding Messages}" />
    <DockPanel Name="ButtonsPanel" Grid.Row="2" Margin="10" Width="500" LastChildFill="False">
      <Button Name="CancelButton" DockPanel.Dock="Right" Content="Cancel" Width="60" Margin="10 0 0 0" Command="{Binding CancelCommand}" />
      <Button Name="OKButton" DockPanel.Dock="Right" Content="OK" Width="60" Margin="10 0 0 0" Command="{Binding OKCommand}" />
    </DockPanel>
  </Grid>
</Window>