<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="Firaxis.MVVMBase.StringAssignerView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="clr-namespace:Firaxis.MVVMBase" MinWidth="640" MinHeight="480" Width="640" Height="480" Background="{DynamicResource GrayBackgroundBrush}" Title="{Binding Title}">
  <FrameworkElement.Resources>
    <ResourceDictionary>
      <Color x:Key="LightGrayBackgroundColor">#FF858585</Color>
      <Color x:Key="GrayBackgroundColor">#FF707070</Color>
      <Color x:Key="DarkGrayColor">#FF464646</Color>
      <Color x:Key="TextColor">#FFFFFFFF</Color>
      <SolidColorBrush x:Key="LightGrayBackgroundBrush" Color="{StaticResource LightGrayBackgroundColor}" />
      <SolidColorBrush x:Key="GrayBackgroundBrush" Color="{StaticResource GrayBackgroundColor}" />
      <SolidColorBrush x:Key="DarkGrayBrush" Color="{StaticResource DarkGrayColor}" />
      <SolidColorBrush x:Key="TextBrush" Color="{StaticResource TextColor}" />
    </ResourceDictionary>
  </FrameworkElement.Resources>
  <DockPanel LastChildFill="True">
    <DockPanel Dock="Bottom" Height="30" LastChildFill="False" HorizontalAlignment="Right" Margin="10">
      <DockPanel>
        <Button Name="okButton" Content="OK" DockPanel.Dock="Left" Margin="2 0" Width="60" Command="{Binding OKCommand}" />
        <Button Name="cancelButton" Content="Cancel" DockPanel.Dock="Left" Margin="2 0" Width="60" Command="{Binding CancelCommand}" />
      </DockPanel>
    </DockPanel>
    <Grid DockPanel.Dock="Top" Margin="10">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="195" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>
      <TextBlock Name="SlotLabel" Grid.Row="0" Grid.Column="0" Foreground="{StaticResource TextBrush}" FontSize="16" Text="{Binding SlotLabel}" />
      <TextBlock Name="DomainLabel" Grid.Row="0" Grid.Column="1" Foreground="{StaticResource TextBrush}" FontSize="16" HorizontalAlignment="Right" Margin="0 0 125 0" Text="{Binding DomainLabel}" />
      <ScrollViewer Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" VerticalScrollBarVisibility="Auto" MaxHeight="600">
        <ItemsControl Name="Slots" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" ItemsSource="{Binding Slots}">
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <local:SlotView Margin="2" />
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </ScrollViewer>
    </Grid>
  </DockPanel>
</Window>