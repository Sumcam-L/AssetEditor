<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="UtilityTools.Views.SourceClassAssociationView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:local="clr-namespace:UtilityTools.Views" Background="#FF707070" SizeToContent="WidthAndHeight" Title="Source File -&gt; Parameter Set Mapping">
  <DockPanel LastChildFill="False" Margin="10">
    <FrameworkElement.Resources>
      <ResourceDictionary>
        <Color x:Key="LightGrayBackgroundColor">#FF858585</Color>
        <Color x:Key="GrayBackgroundColor">#FF707070</Color>
        <Color x:Key="DarkGrayColor">#FF464646</Color>
        <Color x:Key="DarkGrayColorTwo">#FF4C4C4C</Color>
        <Color x:Key="WhiteTextColor">#FFFFFFFF</Color>
        <SolidColorBrush x:Key="LightGrayBackgroundBrush" Color="{StaticResource LightGrayBackgroundColor}" />
        <SolidColorBrush x:Key="GrayBackgroundBrush" Color="{StaticResource GrayBackgroundColor}" />
        <SolidColorBrush x:Key="DarkGrayBrush" Color="{StaticResource DarkGrayColor}" />
        <SolidColorBrush x:Key="ExpanderBackgroundBrush" Color="{StaticResource DarkGrayColorTwo}" />
        <SolidColorBrush x:Key="WhiteTextBrush" Color="{StaticResource WhiteTextColor}" />
        <Style x:Key="BaseLabelStyle" TargetType="{x:Type Label}">
          <Setter Property="HorizontalAlignment" Value="Left" />
          <Setter Property="Margin" Value="0" />
          <Setter Property="MinWidth" Value="50" />
          <Setter Property="Foreground" Value="{StaticResource WhiteTextBrush}" />
          <Setter Property="FontFamily" Value="Segoe UI" />
          <Setter Property="FontSize" Value="12" />
        </Style>
        <Style x:Key="WarningLabelStyle" BasedOn="{StaticResource BaseLabelStyle}" TargetType="{x:Type Label}">
          <Style.Triggers>
            <DataTrigger Value="False" Binding="{Binding AutoMatchFound}">
              <Setter Property="Foreground" Value="#FFFFFF00" />
            </DataTrigger>
            <DataTrigger Value="True" Binding="{Binding AutoMatchFound}">
              <Setter Property="Foreground" Value="#FFFFFFFF" />
            </DataTrigger>
          </Style.Triggers>
        </Style>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <DataTemplate x:Key="ParamSourceClassTemplate">
          <local:ObjectParameterAssignmentFromSourceView HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0 1" />
        </DataTemplate>
      </ResourceDictionary>
    </FrameworkElement.Resources>
    <DockPanel Dock="Top" LastChildFill="False" Visibility="{Binding SourceFilePathVisible, Converter={StaticResource BoolToVis}}">
      <Label Style="{StaticResource BaseLabelStyle}" Content="Source File: " DockPanel.Dock="Left" MinWidth="0" FontWeight="Bold" />
      <Label Style="{StaticResource BaseLabelStyle}" DockPanel.Dock="Left" MinWidth="0" Content="{Binding SourceFilePath}" />
    </DockPanel>
    <TextBlock DockPanel.Dock="Top" Foreground="#FFFFFFFF" FontWeight="Bold" Text="{Binding SourcesMatchedString}" />
    <DockPanel Dock="Top" LastChildFill="False" Margin="0 2">
      <TextBlock Text="Parameter Name" DockPanel.Dock="Left" Foreground="#FFFFFFFF" FontWeight="Bold" MinWidth="{Binding ParameterLabelWidth}" />
      <TextBlock Text="Object Parameter Value" DockPanel.Dock="Left" Margin="2 0 3 0" Foreground="#FFFFFFFF" FontWeight="Bold" MinWidth="{Binding EntityNameEntryWidth}" />
      <TextBlock Text="Source" DockPanel.Dock="Left" Margin="2 0 3 0" Foreground="#FFFFFFFF" FontWeight="Bold" MinWidth="{Binding SourceObjectDropdownWidth}" />
      <TextBlock Text="Object Parameter Class" DockPanel.Dock="Left" Margin="2 0 3 0" Foreground="#FFFFFFFF" FontWeight="Bold" MinWidth="{Binding ClassesDropdownWidth}" />
      <CheckBox Content="Check Out All" TextBlock.Foreground="#FFFFFFFF" DockPanel.Dock="Left" Margin="2 0 3 0" Command="{Binding OverwriteAllCommand}" CommandParameter="{Binding Path=IsChecked, RelativeSource={RelativeSource Self}}" />
    </DockPanel>
    <ScrollViewer DockPanel.Dock="Top" MaxHeight="600" Margin="0 5 0 5">
      <ItemsControl ItemTemplate="{StaticResource ParamSourceClassTemplate}" DockPanel.Dock="Bottom" ItemsSource="{Binding SourceObjects}" />
    </ScrollViewer>
    <DockPanel Dock="Bottom" LastChildFill="False" HorizontalAlignment="Right">
      <Button DockPanel.Dock="Left" Content="OK" Margin="10 0 10 0" Command="{Binding OKCommand}" />
      <Button DockPanel.Dock="Left" Content="Cancel" Command="{Binding CancelCommand}" />
    </DockPanel>
  </DockPanel>
</Window>