<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="UtilityTools.Views.AssetCopierView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:viewModels="clr-namespace:UtilityTools.ViewModels" Title="Asset Copier" Background="#FF707070" SizeToContent="WidthAndHeight" xmlns:views="clr-namespace:UtilityTools.Views">
  <FrameworkElement.Resources>
    <ResourceDictionary>
      <BooleanToVisibilityConverter x:Key="BoolToVis" />
      <Style x:Key="{x:Type CheckBox}" TargetType="{x:Type CheckBox}">
        <Setter Property="TextBlock.Foreground" Value="#FFFFFFFF" />
      </Style>
      <DataTemplate x:Key="BindingContentTemplate">
        <DockPanel>
          <CheckBox Margin="0 1" Content="{Binding DisplayName}" IsChecked="{Binding IsSelected}" />
        </DockPanel>
      </DataTemplate>
      <DataTemplate x:Key="VFXContentTemplate">
        <DockPanel>
          <CheckBox Margin="0 1" Content="{Binding VFXName}" IsChecked="{Binding IsSelected}" />
        </DockPanel>
      </DataTemplate>
      <DataTemplate x:Key="TriggerContentTemplate">
        <DockPanel>
          <CheckBox Margin="0 1" Content="{Binding TriggerName}" IsChecked="{Binding IsSelected}" />
        </DockPanel>
      </DataTemplate>
      <HierarchicalDataTemplate x:Key="{DataTemplateKey {x:Type viewModels:TimelineViewModel}}" x:Name="treeTriggers" DataType="{x:Type viewModels:TimelineViewModel}" ItemTemplate="{StaticResource VFXContentTemplate}" ItemsSource="{Binding Triggers}" />
      <DataTemplate x:Key="TimelineContentTemplate">
        <DockPanel>
          <Expander>
            <HeaderedContentControl.Header>
              <DockPanel>
                <CheckBox DockPanel.Dock="Left" Content="{Binding TimelineName}" IsChecked="{Binding IsSelected}" />
                <TextBlock Text="No Triggers" FontStyle="Italic" Foreground="#FFD3D3D3" DockPanel.Dock="Left" Margin="5 0 0 0" Visibility="{Binding NoChildren, Converter={StaticResource BoolToVis}}" />
              </DockPanel>
            </HeaderedContentControl.Header>
            <ContentControl.Content>
              <ItemsControl Margin="30 0 0 0" ItemTemplate="{StaticResource TriggerContentTemplate}" ItemsSource="{Binding Triggers}" />
            </ContentControl.Content>
          </Expander>
        </DockPanel>
      </DataTemplate>
    </ResourceDictionary>
  </FrameworkElement.Resources>
  <DockPanel LastChildFill="False">
    <DockPanel Dock="Top" Margin="5 5 0 0">
      <DockPanel Dock="Left">
        <TextBlock Text="Source Asset: " DockPanel.Dock="Left" Foreground="#FFFFFFFF" FontWeight="SemiBold" TextDecorations="Underline" />
        <TextBlock DockPanel.Dock="Left" Margin="2 0 0 0" Foreground="#FFFFFFFF" Text="{Binding SourceAssetName}" />
      </DockPanel>
      <DockPanel Dock="Left" Margin="10 0 0 0">
        <TextBlock Text="Target Assets: " DockPanel.Dock="Left" Foreground="#FFFFFFFF" FontWeight="SemiBold" TextDecorations="Underline" />
        <ItemsControl DockPanel.Dock="Left" Margin="2 0 0 0" Foreground="#FFFFFFFF" ItemsSource="{Binding TargetAssetNames}">
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <TextBlock Text="{Binding}" />
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </DockPanel>
    </DockPanel>
    <DockPanel Dock="Bottom" Height="30" LastChildFill="False" HorizontalAlignment="Right">
      <DockPanel>
        <Button Name="okButton" Content="OK" DockPanel.Dock="Left" Margin="2 0" Width="60" Command="{Binding OKCommand}" />
        <Button Name="cancelButton" Content="Cancel" DockPanel.Dock="Left" Margin="2 0" Width="60" Command="{Binding CancelCommand}" />
      </DockPanel>
    </DockPanel>
    <DockPanel Dock="Top" Margin="10" LastChildFill="False">
      <DockPanel Dock="Left" Background="#FF4c4c4c">
        <CheckBox Content="Animation Bindings" DockPanel.Dock="Top" TextBlock.FontWeight="SemiBold" Margin="5 5 5 0" Command="{Binding CheckAnimationBindings}" CommandParameter="{Binding Path=IsChecked, RelativeSource={RelativeSource Self}}" />
        <Rectangle Height="1" Fill="#FFFFFFFF" Margin="1" DockPanel.Dock="Top" />
        <ItemsControl ItemTemplate="{StaticResource BindingContentTemplate}" DockPanel.Dock="Top" Margin="5" ItemsSource="{Binding AnimationBindings}" />
      </DockPanel>
      <DockPanel Dock="Left" Margin="10 0 0 0" Background="#FF4c4c4c">
        <CheckBox Content="Particle Effects" DockPanel.Dock="Top" TextBlock.FontWeight="SemiBold" Margin="5 5 5 0" Command="{Binding CheckParticleEffects}" CommandParameter="{Binding Path=IsChecked, RelativeSource={RelativeSource Self}}" />
        <Rectangle Height="1" Fill="#FFFFFFFF" Margin="1" DockPanel.Dock="Top" />
        <ItemsControl ItemTemplate="{StaticResource VFXContentTemplate}" DockPanel.Dock="Top" Margin="5" ItemsSource="{Binding ParticleEffects}" />
      </DockPanel>
      <DockPanel Dock="Left" Margin="10 0 0 0" Background="#FF4c4c4c">
        <CheckBox Content="Timeline Bindings" DockPanel.Dock="Top" TextBlock.FontWeight="SemiBold" Margin="5 5 5 0" Command="{Binding CheckTimelines}" CommandParameter="{Binding Path=IsChecked, RelativeSource={RelativeSource Self}}" />
        <Rectangle Height="1" Fill="#FFFFFFFF" Margin="1" DockPanel.Dock="Top" />
        <ItemsControl ItemTemplate="{StaticResource BindingContentTemplate}" DockPanel.Dock="Top" Margin="5" ItemsSource="{Binding TimelineBindings}" />
      </DockPanel>
      <DockPanel Dock="Left" Margin="10 0 0 0" Background="#FF4c4c4c">
        <CheckBox Content="Triggers" DockPanel.Dock="Top" TextBlock.FontWeight="SemiBold" Margin="5 5 5 0" Command="{Binding CheckTriggers}" CommandParameter="{Binding Path=IsChecked, RelativeSource={RelativeSource Self}}" />
        <Rectangle Height="1" Fill="#FFFFFFFF" Margin="1" DockPanel.Dock="Top" />
        <ScrollViewer>
          <ItemsControl ItemTemplate="{StaticResource TimelineContentTemplate}" DockPanel.Dock="Top" Margin="5" ItemsSource="{Binding Timelines}" />
        </ScrollViewer>
      </DockPanel>
    </DockPanel>
  </DockPanel>
</Window>