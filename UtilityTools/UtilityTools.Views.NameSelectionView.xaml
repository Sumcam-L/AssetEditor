<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="UtilityTools.Views.NameSelectionView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" SizeToContent="WidthAndHeight" Background="#FF707070" MinHeight="100" MinWidth="100" xmlns:views="clr-namespace:UtilityTools.Views;assembly=UtilityTools" Title="{Binding Title}">
  <DockPanel LastChildFill="False" Margin="5">
    <Label DockPanel.Dock="Top" Margin="5 5 0 0" Foreground="#FFFFFFFF" Content="{Binding NamesLabel}" />
    <ItemsControl DockPanel.Dock="Top" Margin="5" ItemsSource="{Binding Names}">
      <ItemsControl.ItemTemplate>
        <DataTemplate>
          <DockPanel>
            <CheckBox Margin="1" TextBlock.Foreground="#FFFFFFFF" Content="{Binding Name}" IsChecked="{Binding IsSelected}" />
          </DockPanel>
        </DataTemplate>
      </ItemsControl.ItemTemplate>
    </ItemsControl>
    <DockPanel Dock="Bottom" LastChildFill="False">
      <Button Name="OKButton" Content="OK" DockPanel.Dock="Left" Margin="5" Command="{Binding OKCommand}" />
      <Button Name="CancelButton" Content="Cancel" DockPanel.Dock="Left" Margin="5" Command="{Binding CancelCommand}" />
    </DockPanel>
  </DockPanel>
</Window>