<?xml version="1.0" encoding="utf-8"?>
<c:CommonDialog x:Class="Sce.Atf.Wpf.Applications.VersionControl.ReconcileDialog" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:c="clr-namespace:Sce.Atf.Wpf.Controls" xmlns:l="clr-namespace:Sce.Atf.Wpf" xmlns:vm="clr-namespace:Sce.Atf.Wpf.Applications.VersionControl" xmlns:m="clr-namespace:Sce.Atf.Wpf.Markup" ShowInTaskbar="False">
  <DockPanel LastChildFill="True">
    <DockPanel Dock="Bottom" LastChildFill="False" Margin="0,12,0,0">
      <Button DockPanel.Dock="Right" IsCancel="True" Style="{StaticResource {x:Static l:Resources.DialogButtonStyleKey}}" Command="{Binding CancelCommand}" Content="{m:Loc Cancel}" />
      <Button DockPanel.Dock="Right" IsDefault="True" Style="{StaticResource {x:Static l:Resources.DialogButtonStyleKey}}" Command="{Binding OkCommand}" Content="{m:Loc Reconcile}" />
    </DockPanel>
    <Grid Margin="8">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>
      <TextBlock Grid.Row="0" Margin="2" Text="{m:Loc Modified files (Select files to check out)}" />
      <ListBox Grid.Row="1" Margin="4" ScrollViewer.CanContentScroll="False" SelectionMode="Extended" AllowDrop="False" HorizontalContentAlignment="Stretch" ItemsSource="{Binding Modified}">
        <ItemsControl.ItemContainerStyle>
          <Style BasedOn="{StaticResource {x:Type ListBoxItem}}" TargetType="{x:Type ListBoxItem}">
            <Setter Property="ListBoxItem.IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
          </Style>
        </ItemsControl.ItemContainerStyle>
        <ItemsControl.ItemTemplate>
          <DataTemplate DataType="{x:Type vm:CheckableItem}">
            <StackPanel Orientation="Horizontal">
              <CheckBox IsChecked="{Binding IsChecked}" />
              <TextBlock Text="{Binding LocalPath}" />
            </StackPanel>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ListBox>
      <TextBlock Grid.Row="2" Margin="2" Text="{m:Loc Local files not in depot (Select files to mark for add)}" />
      <ListBox Grid.Row="3" Margin="4" ScrollViewer.CanContentScroll="False" SelectionMode="Extended" AllowDrop="False" HorizontalContentAlignment="Stretch" ItemsSource="{Binding NotInDepot}">
        <ItemsControl.ItemContainerStyle>
          <Style BasedOn="{StaticResource {x:Type ListBoxItem}}" TargetType="{x:Type ListBoxItem}">
            <Setter Property="ListBoxItem.IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
          </Style>
        </ItemsControl.ItemContainerStyle>
        <ItemsControl.ItemTemplate>
          <DataTemplate DataType="{x:Type vm:CheckableItem}">
            <StackPanel Orientation="Horizontal">
              <CheckBox IsChecked="{Binding IsChecked}" />
              <TextBlock Text="{Binding LocalPath}" />
            </StackPanel>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ListBox>
    </Grid>
  </DockPanel>
</c:CommonDialog>