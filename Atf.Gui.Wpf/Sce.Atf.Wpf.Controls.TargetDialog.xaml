<?xml version="1.0" encoding="utf-8"?>
<c:CommonDialog x:Class="Sce.Atf.Wpf.Controls.TargetDialog" x:ClassModifier="internal" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:l="clr-namespace:Sce.Atf.Wpf" xmlns:c="clr-namespace:Sce.Atf.Wpf.Controls" xmlns:m="clr-namespace:Sce.Atf.Wpf.Markup" xmlns:b="clr-namespace:Sce.Atf.Wpf.Behaviors" ShowInTaskbar="False" Height="200" Width="600" MinWidth="600" MinHeight="200" ResizeMode="CanResizeWithGrip" Title="{m:Loc Targets}">
  <FrameworkElement.Resources>
    <ResourceDictionary>
      <l:CommandReference x:Key="DeleteCommandRef" Command="{Binding DeleteTargetCommand}" />
      <l:CommandReference x:Key="EditUserTargetCommandRef" Command="{Binding EditUserTargetCommand}" />
      <ContextMenu x:Key="itemContextMenu">
        <MenuItem Command="{Binding Path=DataContext.AddUserTargetCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}" Header="{m:Loc Add}" />
        <MenuItem Command="{Binding Path=DataContext.EditUserTargetCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}" Header="{m:Loc Edit}" />
        <MenuItem Command="{Binding Path=DataContext.DeleteTargetCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}" Header="{m:Loc Delete}" />
      </ContextMenu>
      <Style x:Key="listViewItem" TargetType="{x:Type ListViewItem}" BasedOn="{StaticResource {x:Static GridView.GridViewItemContainerStyleKey}}">
        <Setter Property="b:ThemeStyleBehavior.AutoMergeStyle" Value="True" />
        <Setter Property="ListBoxItem.IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
        <Setter Property="FrameworkElement.ContextMenu" Value="{StaticResource itemContextMenu}" />
        <Setter Property="b:CommandBehavior.Command" Value="{Binding ElementName=listView, Path=DataContext.EditUserTargetCommand}" />
        <Setter Property="b:CommandBehavior.RoutedEventName" Value="MouseDoubleClick" />
      </Style>
    </ResourceDictionary>
  </FrameworkElement.Resources>
  <DockPanel>
    <DockPanel Dock="Bottom" LastChildFill="False" Margin="12">
      <Button Margin="0" Style="{StaticResource {x:Static l:Resources.DialogButtonStyleKey}}" Content="{m:Loc Find Targets}" Command="{Binding FindTargetsCommand}" />
      <Button Style="{StaticResource {x:Static l:Resources.DialogButtonStyleKey}}" Content="{m:Loc Add}" Command="{Binding AddUserTargetCommand}" />
      <Button Style="{StaticResource {x:Static l:Resources.DialogButtonStyleKey}}" Content="{m:Loc Edit}" Command="{Binding EditUserTargetCommand}" />
      <Button Style="{StaticResource {x:Static l:Resources.DialogButtonStyleKey}}" Content="{m:Loc Delete}" Command="{Binding DeleteTargetCommand}" />
      <ComboBox SelectedIndex="0" Margin="6,0,0,0" IsSynchronizedWithCurrentItem="True" Width="120" ItemsSource="{Binding Protocols}" IsEnabled="{Binding Path=HasItems, Mode=OneWay, RelativeSource={RelativeSource Self}}">
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <TextBlock Text="{Binding Name}" />
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ComboBox>
      <Button IsDefault="True" DockPanel.Dock="Right" Style="{StaticResource {x:Static l:Resources.DialogButtonStyleKey}}" Content="{m:Loc OK}" Command="{Binding OkCommand}" />
    </DockPanel>
    <ListView Name="listView" DockPanel.Dock="Top" Margin="8,4,8,0" ItemContainerStyle="{DynamicResource listViewItem}" IsSynchronizedWithCurrentItem="True" ItemsSource="{Binding Targets}">
      <FrameworkElement.ContextMenu>
        <ContextMenu>
          <MenuItem Command="{Binding AddUserTargetCommand}" Header="{m:Loc Add}" />
        </ContextMenu>
      </FrameworkElement.ContextMenu>
      <UIElement.InputBindings>
        <KeyBinding Key="Delete" Command="{StaticResource DeleteCommandRef}" />
        <KeyBinding Key="Enter" Command="{StaticResource EditUserTargetCommandRef}" />
      </UIElement.InputBindings>
      <ListView.View>
        <GridView>
          <GridViewColumn Width="24">
            <GridViewColumn.CellTemplate>
              <DataTemplate>
                <RadioButton GroupName="targetsRadioGroup" IsChecked="{Binding IsSelected, Mode=TwoWay}" />
              </DataTemplate>
            </GridViewColumn.CellTemplate>
          </GridViewColumn>
          <GridViewColumn Width="100" Header="{m:Loc Target}" DisplayMemberBinding="{Binding Target.Name}" />
          <GridViewColumn Width="120" Header="{m:Loc Host}" DisplayMemberBinding="{Binding Target.Host}" />
          <GridViewColumn Width="120" Header="{m:Loc Protocol}" DisplayMemberBinding="{Binding Protocol.Name}" />
          <GridViewColumn Width="60" Header="{m:Loc Status}" DisplayMemberBinding="{Binding Target.Status}" />
          <GridViewColumn Width="80" Header="{m:Loc Info}" DisplayMemberBinding="{Binding Target.ConnectionInfo}" />
        </GridView>
      </ListView.View>
    </ListView>
  </DockPanel>
</c:CommonDialog>