<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:s="clr-namespace:System;assembly=mscorlib" xmlns:local="clr-namespace:Sce.Atf.Wpf.Docking" xmlns:vc="clr-namespace:Sce.Atf.Wpf.ValueConverters">
  <vc:NullVisibilityConverter x:Key="ConvertNullToVisibility" />
  <DataTemplate x:Key="HeaderTemplate">
    <StackPanel Orientation="Horizontal">
      <Image Width="16" Height="16" Margin="0,0,4,0" Source="{Binding Path=Icon}" Visibility="{Binding Path=Icon, Converter={StaticResource ConvertNullToVisibility}}" />
      <TextBlock Text="{Binding Path=Header}" />
    </StackPanel>
  </DataTemplate>
  <DataTemplate x:Key="ContentTemplate">
    <ContentControl Content="{Binding Path=DataContext.Content, RelativeSource={RelativeSource AncestorType={x:Type ContentPresenter}}}" DataContext="{x:Null}" />
  </DataTemplate>
  <Style x:Key="TabItemStyle" TargetType="{x:Type TabItem}">
    <Setter Property="FrameworkElement.FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="TextElement.Foreground" Value="#FF000000" />
    <Setter Property="Padding" Value="6,1,6,1" />
    <Setter Property="Border.BorderBrush" Value="#FFC0C0C0" />
    <Setter Property="Panel.Background" Value="{Binding Path=Background, RelativeSource={RelativeSource AncestorType=local:TabLayout}}" />
    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    <Setter Property="VerticalContentAlignment" Value="Stretch" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type TabItem}">
          <Border SnapsToDevicePixels="True">
            <Grid Margin="0,0,1,0">
              <Grid Name="Transform">
                <Border CornerRadius="0" Background="#FFFFFFFF" />
                <Border Name="Bd" BorderThickness="{TemplateBinding Border.BorderThickness}" CornerRadius="0" BorderBrush="{TemplateBinding Border.BorderBrush}" Background="#FFD3D3D3" />
                <Border Name="HighlightBorder" BorderThickness="1,1,1,0" CornerRadius="0,0,0,0" Height="3" VerticalAlignment="Top" />
              </Grid>
              <Border BorderThickness="{TemplateBinding Border.BorderThickness}" Padding="{TemplateBinding Control.Padding}">
                <ContentPresenter RecognizesAccessKey="True" Content="{TemplateBinding HeaderedContentControl.Header}" ContentTemplate="{TemplateBinding HeaderedContentControl.HeaderTemplate}" ContentStringFormat="{TemplateBinding HeaderedContentControl.HeaderStringFormat}" ContentSource="Header" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" />
              </Border>
            </Grid>
          </Border>
          <ControlTemplate.Triggers>
            <DataTrigger Value="1" Binding="{Binding Path=Items.Count, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type TabControl}}}">
              <Setter Property="Visibility" Value="Collapsed" />
            </DataTrigger>
            <Trigger Property="UIElement.IsMouseOver" Value="True">
              <Setter TargetName="Bd" Property="Panel.Background">
                <Setter.Value>
                  <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientBrush.GradientStops>
                      <GradientStop Color="#FFFFFFFF" Offset="0" />
                      <GradientStop Color="#FFECEBE6" Offset="1" />
                    </GradientBrush.GradientStops>
                  </LinearGradientBrush>
                </Setter.Value>
              </Setter>
            </Trigger>
            <Trigger Property="Selector.IsSelected" Value="True">
              <Setter Property="Panel.ZIndex" Value="1" />
              <Setter TargetName="Bd" Property="Panel.Background" Value="{Binding Background, RelativeSource={RelativeSource AncestorType=TabItem}}" />
            </Trigger>
            <Trigger Property="TabItem.TabStripPlacement" Value="Bottom">
              <Setter TargetName="Transform" Property="FrameworkElement.LayoutTransform">
                <Setter.Value>
                  <ScaleTransform ScaleX="1" ScaleY="-1" />
                </Setter.Value>
              </Setter>
            </Trigger>
            <Trigger Property="TabItem.TabStripPlacement" Value="Left">
              <Setter TargetName="Transform" Property="FrameworkElement.LayoutTransform">
                <Setter.Value>
                  <RotateTransform Angle="-90" />
                </Setter.Value>
              </Setter>
            </Trigger>
            <Trigger Property="TabItem.TabStripPlacement" Value="Right">
              <Setter TargetName="Transform" Property="FrameworkElement.LayoutTransform">
                <Setter.Value>
                  <RotateTransform Angle="90" />
                </Setter.Value>
              </Setter>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Value="{x:Static SystemColors.GrayTextBrush}" Property="TextElement.Foreground" />
              <Setter TargetName="Bd" Property="Panel.Background" Value="#FFF5F4EA" />
              <Setter TargetName="Bd" Property="Border.BorderBrush" Value="#FFC9C7BA" />
            </Trigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="Selector.IsSelected" Value="True" />
                <Condition Value="{x:Static Dock.Top}" Property="TabItem.TabStripPlacement" />
              </MultiTrigger.Conditions>
              <Setter Property="Margin" Value="-2,-2,-2,-1" />
            </MultiTrigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="Selector.IsSelected" Value="True" />
                <Condition Value="{x:Static Dock.Bottom}" Property="TabItem.TabStripPlacement" />
              </MultiTrigger.Conditions>
              <Setter Property="Margin" Value="-2,-1,-2,-2" />
            </MultiTrigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="Selector.IsSelected" Value="True" />
                <Condition Value="{x:Static Dock.Left}" Property="TabItem.TabStripPlacement" />
              </MultiTrigger.Conditions>
              <Setter Property="Margin" Value="-2,-2,-1,-2" />
            </MultiTrigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="Selector.IsSelected" Value="True" />
                <Condition Value="{x:Static Dock.Right}" Property="TabItem.TabStripPlacement" />
              </MultiTrigger.Conditions>
              <Setter Property="Margin" Value="-1,-2,-2,-2" />
            </MultiTrigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{x:Type local:TabLayout}" TargetType="{x:Type local:TabLayout}">
    <Setter Property="TabControl.TabStripPlacement" Value="Bottom" />
    <Setter Property="TextElement.Foreground" Value="{x:Static SystemColors.ControlTextBrush}" />
    <Setter Property="Border.BorderBrush" Value="#FFC0C0C0" />
    <Setter Property="Panel.Background" Value="{Binding Path=Background, RelativeSource={RelativeSource AncestorType=local:DockedWindow}}" />
    <Setter Property="HorizontalContentAlignment" Value="Center" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="local:TabLayout.ItemStyle" Value="{StaticResource TabItemStyle}" />
    <Setter Property="ItemTemplate" Value="{StaticResource HeaderTemplate}" />
    <Setter Property="TabControl.ContentTemplate" Value="{StaticResource ContentTemplate}" />
    <Setter Property="ItemsSource" Value="{Binding Children}" />
    <Setter Property="FrameworkElement.FocusVisualStyle" Value="{x:Null}" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type local:TabLayout}">
          <Grid ClipToBounds="True" SnapsToDevicePixels="True" KeyboardNavigation.TabNavigation="Local">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Name="ColumnDefinition0" />
              <ColumnDefinition Name="ColumnDefinition1" Width="0" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
              <RowDefinition Name="RowDefinition0" Height="Auto" />
              <RowDefinition Name="RowDefinition1" Height="*" />
            </Grid.RowDefinitions>
            <Border Name="TabPanelBorder" BorderBrush="{TemplateBinding Border.BorderBrush}" BorderThickness="0,0,0,1" Background="{TemplateBinding Panel.Background}" Grid.Column="0" Grid.Row="0" Panel.ZIndex="1" Margin="2,0,2,0">
              <TabPanel Name="HeaderPanel" IsItemsHost="True" Margin="4,4,4,0" KeyboardNavigation.TabIndex="1" Grid.Column="0" Grid.Row="0" Background="#00FFFFFF" />
            </Border>
            <Border Name="ContentPanel" BorderThickness="{TemplateBinding Border.BorderThickness}" BorderBrush="{TemplateBinding Border.BorderBrush}" Background="{TemplateBinding Panel.Background}" KeyboardNavigation.TabIndex="2" KeyboardNavigation.TabNavigation="Local" KeyboardNavigation.DirectionalNavigation="Contained" Grid.Column="0" Grid.Row="1">
              <ContentPresenter Name="PART_SelectedContentHost" Content="{TemplateBinding TabControl.SelectedContent}" ContentTemplate="{TemplateBinding TabControl.SelectedContentTemplate}" ContentStringFormat="{TemplateBinding TabControl.SelectedContentStringFormat}" ContentSource="SelectedContent" Margin="{TemplateBinding Control.Padding}" SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" />
            </Border>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="TabControl.TabStripPlacement" Value="Bottom">
              <Setter TargetName="TabPanelBorder" Property="Grid.Row" Value="1" />
              <Setter TargetName="ContentPanel" Property="Grid.Row" Value="0" />
              <Setter TargetName="RowDefinition0" Property="RowDefinition.Height" Value="*" />
              <Setter TargetName="RowDefinition1" Property="RowDefinition.Height" Value="Auto" />
              <Setter TargetName="HeaderPanel" Property="Margin" Value="4,0,4,4" />
              <Setter TargetName="TabPanelBorder" Property="Margin" Value="2,0,2,0" />
              <Setter TargetName="TabPanelBorder" Property="Border.BorderThickness" Value="0,1,0,0" />
            </Trigger>
            <Trigger Property="TabControl.TabStripPlacement" Value="Left">
              <Setter TargetName="TabPanelBorder" Property="Grid.Row" Value="0" />
              <Setter TargetName="ContentPanel" Property="Grid.Row" Value="0" />
              <Setter TargetName="TabPanelBorder" Property="Grid.Column" Value="0" />
              <Setter TargetName="ContentPanel" Property="Grid.Column" Value="1" />
              <Setter TargetName="ColumnDefinition0" Property="ColumnDefinition.Width" Value="Auto" />
              <Setter TargetName="ColumnDefinition1" Property="ColumnDefinition.Width" Value="*" />
              <Setter TargetName="RowDefinition0" Property="RowDefinition.Height" Value="*" />
              <Setter TargetName="RowDefinition1" Property="RowDefinition.Height" Value="0" />
              <Setter TargetName="HeaderPanel" Property="Margin" Value="4,4,0,4" />
              <Setter TargetName="TabPanelBorder" Property="Margin" Value="0,2,0,2" />
              <Setter TargetName="TabPanelBorder" Property="Border.BorderThickness" Value="0,0,1,0" />
            </Trigger>
            <Trigger Property="TabControl.TabStripPlacement" Value="Right">
              <Setter TargetName="TabPanelBorder" Property="Grid.Row" Value="0" />
              <Setter TargetName="ContentPanel" Property="Grid.Row" Value="0" />
              <Setter TargetName="TabPanelBorder" Property="Grid.Column" Value="1" />
              <Setter TargetName="ContentPanel" Property="Grid.Column" Value="0" />
              <Setter TargetName="ColumnDefinition0" Property="ColumnDefinition.Width" Value="*" />
              <Setter TargetName="ColumnDefinition1" Property="ColumnDefinition.Width" Value="Auto" />
              <Setter TargetName="RowDefinition0" Property="RowDefinition.Height" Value="*" />
              <Setter TargetName="RowDefinition1" Property="RowDefinition.Height" Value="0" />
              <Setter TargetName="HeaderPanel" Property="Margin" Value="0,4,4,4" />
              <Setter TargetName="TabPanelBorder" Property="Margin" Value="0,2,0,2" />
              <Setter TargetName="TabPanelBorder" Property="Border.BorderThickness" Value="1,0,0,0" />
            </Trigger>
            <Trigger Property="local:TabLayout.ItemsCount" Value="1">
              <Setter TargetName="TabPanelBorder" Property="Border.BorderThickness" Value="0" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Value="{x:Static SystemColors.GrayTextBrush}" Property="TextElement.Foreground" />
            </Trigger>
            <Trigger Property="local:TabLayout.ItemsCount" Value="1">
              <Setter Property="Padding" Value="0" />
              <Setter TargetName="HeaderPanel" Property="Margin" Value="0" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>