<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:forms="clr-namespace:System.Windows.Forms;assembly=System.Windows.Forms" xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity" xmlns:l="clr-namespace:Sce.Atf.Wpf" xmlns:b="clr-namespace:Sce.Atf.Wpf.Behaviors" xmlns:m="clr-namespace:Sce.Atf.Wpf.Models" xmlns:ct="clr-namespace:Sce.Atf.Wpf.Controls" xmlns:c="clr-namespace:Sce.Atf.Wpf.ValueConverters" xmlns:markup="clr-namespace:Sce.Atf.Wpf.Markup" xmlns:sys="clr-namespace:System;assembly=mscorlib">
  <Style x:Key="{x:Static l:Resources.AutoGreyStyleKey}" TargetType="{x:Type FrameworkElement}">
    <Style.Triggers>
      <Trigger Property="IsEnabled" Value="False">
        <Setter Property="UIElement.Opacity" Value="0.33" />
      </Trigger>
    </Style.Triggers>
  </Style>
  <Style x:Key="{x:Static l:Resources.DialogButtonStyleKey}" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
    <Setter Property="b:ThemeStyleBehavior.AutoMergeStyle" Value="True" />
    <Setter Property="MinWidth" Value="75" />
    <Setter Property="MinHeight" Value="23" />
    <Setter Property="Margin" Value="6,0,0,0" />
  </Style>
  <DataTemplate x:Key="{DataTemplateKey {x:Type forms:Control}}" DataType="{x:Type forms:Control}">
    <ct:WindowsFormsHostEx BoundChild="{Binding}" />
  </DataTemplate>
  <DataTemplate x:Key="{DataTemplateKey {x:Type m:ToolBarSeparator}}" DataType="{x:Type m:ToolBarSeparator}">
    <Separator Style="{DynamicResource {x:Static ToolBar.SeparatorStyleKey}}" />
  </DataTemplate>
  <DataTemplate x:Key="{x:Static l:Resources.ToolBarItemTemplateKey}">
    <Button Style="{DynamicResource {x:Static ToolBar.ButtonStyleKey}}" Command="{Binding}" ToolTip="{Binding Description}">
      <ct:Icon Width="16" Height="16" SnapsToDevicePixels="True" Style="{StaticResource {x:Static l:Resources.AutoGreyStyleKey}}" SourceKey="{Binding ImageSourceKey}" />
    </Button>
  </DataTemplate>
  <Style x:Key="{x:Static l:Resources.ToolBarStyleKey}" TargetType="{x:Type ToolBar}" BasedOn="{StaticResource {x:Type ToolBar}}">
    <Setter Property="b:ThemeStyleBehavior.AutoMergeStyle" Value="True" />
    <Setter Property="MinHeight" Value="16" />
  </Style>
  <Style x:Key="{x:Static l:Resources.ToolBarTrayStyleKey}" TargetType="{x:Type ToolBarTray}" BasedOn="{StaticResource {x:Type ToolBarTray}}">
    <Setter Property="b:ThemeStyleBehavior.AutoMergeStyle" Value="True" />
    <Setter Property="UIElement.AllowDrop" Value="False" />
    <Setter Property="VerticalAlignment" Value="Top" />
  </Style>
  <Style x:Key="{x:Type MenuItem}" TargetType="{x:Type MenuItem}">
    <Setter Property="HorizontalContentAlignment" Value="Left" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
  </Style>
  <Style x:Key="{x:Static l:Resources.MenuItemImageStyleKey}" TargetType="{x:Type Image}" BasedOn="{StaticResource {x:Static l:Resources.AutoGreyStyleKey}}">
    <Setter Property="Width" Value="12" />
    <Setter Property="Height" Value="12" />
  </Style>
  <Style x:Key="{x:Static l:Resources.SubMenuItemStyleKey}" TargetType="{x:Type MenuItem}" BasedOn="{StaticResource {x:Type MenuItem}}">
    <Setter Property="b:ThemeStyleBehavior.AutoMergeStyle" Value="True" />
    <Setter Property="Header" Value="{Binding Text}" />
    <Setter Property="HorizontalContentAlignment" Value="Left" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="ItemsSource" Value="{Binding Children}" />
    <Setter Property="Visibility" Value="{Binding IsVisible, Converter={c:BoolToVisibilityConverter}}" />
  </Style>
  <Style x:Key="{x:Static l:Resources.CommandMenuItemStyleKey}" TargetType="{x:Type MenuItem}" BasedOn="{StaticResource {x:Type MenuItem}}">
    <Setter Property="b:ThemeStyleBehavior.AutoMergeStyle" Value="True" />
    <Setter Property="Header" Value="{Binding Text}" />
    <Setter Property="MenuItem.Command" Value="{Binding}" />
    <Setter Property="HorizontalContentAlignment" Value="Left" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="MenuItem.IsChecked" Value="{Binding IsChecked}" />
    <Setter Property="MenuItem.InputGestureText" Value="{Binding Shortcuts, Converter={c:InputGestureTextConverter}}" />
    <Setter Property="MenuItem.Icon" Value="{Binding ImageSourceKey, Converter={c:ImageResourceLookupConverter}}" />
  </Style>
  <Style x:Key="{x:Static l:Resources.MenuSeparatorStyleKey}" TargetType="{x:Type MenuItem}" BasedOn="{StaticResource {x:Type MenuItem}}">
    <Setter Property="b:ThemeStyleBehavior.AutoMergeStyle" Value="True" />
    <Setter Property="HorizontalContentAlignment" Value="Left" />
    <Setter Property="VerticalContentAlignment" Value="Center" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate>
          <Separator Style="{DynamicResource {x:Static MenuItem.SeparatorStyleKey}}" />
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <c:MenuItemStyleSelector x:Key="MenuStyleSelector" />
  <Style x:Key="{x:Static l:Resources.MenuStyleKey}" TargetType="{x:Type Menu}" BasedOn="{StaticResource {x:Type Menu}}">
    <Style.Setters>
      <Setter Property="b:ThemeStyleBehavior.AutoMergeStyle" Value="True" />
      <Setter Property="ItemContainerStyleSelector" Value="{StaticResource MenuStyleSelector}" />
      <Setter Property="UIElement.AllowDrop" Value="False" />
      <Setter Property="FontFamily" Value="{DynamicResource {x:Static SystemFonts.MessageFontFamilyKey}}" />
      <Setter Property="FontSize" Value="{DynamicResource {x:Static SystemFonts.MessageFontSizeKey}}" />
      <Setter Property="FontWeight" Value="{DynamicResource {x:Static SystemFonts.MessageFontWeightKey}}" />
      <Setter Property="Visibility" Value="{Binding IsVisible, Converter={c:BoolToVisibilityConverter}}" />
    </Style.Setters>
  </Style>
  <Style x:Key="{x:Static l:Resources.ContextMenuStyleKey}" TargetType="{x:Type ContextMenu}" BasedOn="{StaticResource {x:Type ContextMenu}}">
    <Style.Setters>
      <Setter Property="b:ThemeStyleBehavior.AutoMergeStyle" Value="True" />
      <Setter Property="ItemContainerStyleSelector" Value="{StaticResource MenuStyleSelector}" />
      <Setter Property="UIElement.AllowDrop" Value="False" />
      <Setter Property="FontFamily" Value="{DynamicResource {x:Static SystemFonts.MessageFontFamilyKey}}" />
      <Setter Property="FontSize" Value="{DynamicResource {x:Static SystemFonts.MessageFontSizeKey}}" />
      <Setter Property="FontWeight" Value="{DynamicResource {x:Static SystemFonts.MessageFontWeightKey}}" />
    </Style.Setters>
  </Style>
  <Style x:Key="{x:Static l:Resources.DialogRootBorderStyleKey}" TargetType="{x:Type Border}">
    <Setter Property="Border.Padding" Value="4" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="TextElement.FontFamily" Value="{DynamicResource {x:Static SystemFonts.MessageFontFamilyKey}}" />
    <Setter Property="TextElement.FontSize" Value="{DynamicResource {x:Static SystemFonts.MessageFontSizeKey}}" />
    <Setter Property="TextElement.FontWeight" Value="{DynamicResource {x:Static SystemFonts.MessageFontWeightKey}}" />
    <Setter Property="UIElement.SnapsToDevicePixels" Value="True" />
  </Style>
  <DataTemplate x:Key="{DataTemplateKey {x:Type m:StatusText}}" DataType="{x:Type m:StatusText}">
    <TextBlock Text="{Binding Text}" ToolTip="{Binding ToolTip}" MinWidth="{Binding MinWidth, Mode=OneTime}">
      <FrameworkElement.Style>
        <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource {x:Type TextBlock}}">
          <Setter Property="b:ThemeStyleBehavior.AutoMergeStyle" Value="True" />
          <Setter Property="Foreground" Value="{Binding ForeColor}" />
          <Style.Triggers>
            <DataTrigger Binding="{Binding ForeColor}" Value="{x:Null}">
              <Setter Property="Foreground" Value="{DynamicResource Text1Brush}" />
            </DataTrigger>
          </Style.Triggers>
        </Style>
      </FrameworkElement.Style>
    </TextBlock>
  </DataTemplate>
  <DataTemplate x:Key="{DataTemplateKey {x:Type m:StatusImage}}" DataType="{x:Type m:StatusImage}">
    <Image Width="12" Height="12" Source="{Binding ImageSourceKey, Converter={c:ResourceLookupConverter}}" />
  </DataTemplate>
  <DataTemplate x:Key="{DataTemplateKey {x:Type m:ProgressViewModel}}" DataType="{x:Type m:ProgressViewModel}">
    <ProgressBar Height="16" VerticalAlignment="Center" Width="100" ToolTip="{Binding Description}" Value="{Binding Progress, Mode=OneWay}" IsIndeterminate="{Binding IsIndeterminate}" />
  </DataTemplate>
  <Style x:Key="{x:Static l:Resources.StatusBarItemStyleKey}" TargetType="{x:Type StatusBarItem}" BasedOn="{StaticResource {x:Type StatusBarItem}}">
    <Setter Property="b:ThemeStyleBehavior.AutoMergeStyle" Value="True" />
    <Style.Triggers>
      <DataTrigger Value="False" Binding="{Binding IsLeftDock, FallbackValue=False}">
        <Setter Property="DockPanel.Dock" Value="Right" />
        <Setter Property="HorizontalContentAlignment" Value="Right" />
      </DataTrigger>
      <DataTrigger Binding="{Binding}" Value="{x:Null}">
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate>
              <Separator Style="{DynamicResource {x:Static StatusBar.SeparatorStyleKey}}" />
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </DataTrigger>
    </Style.Triggers>
  </Style>
  <Style x:Key="{x:Static l:Resources.StatusBarStyleKey}" TargetType="{x:Type StatusBar}" BasedOn="{StaticResource {x:Type StatusBar}}">
    <Setter Property="b:ThemeStyleBehavior.AutoMergeStyle" Value="True" />
    <Setter Property="ItemContainerStyle" Value="{DynamicResource {x:Static l:Resources.StatusBarItemStyleKey}}" />
    <Setter Property="UIElement.AllowDrop" Value="False" />
    <Setter Property="FontFamily" Value="{DynamicResource {x:Static SystemFonts.MessageFontFamilyKey}}" />
    <Setter Property="FontSize" Value="{DynamicResource {x:Static SystemFonts.MessageFontSizeKey}}" />
    <Setter Property="FontWeight" Value="{DynamicResource {x:Static SystemFonts.MessageFontWeightKey}}" />
  </Style>
  <Style x:Key="{x:Static l:Resources.ListViewItemStyleKey}" TargetType="{x:Type ListViewItem}" BasedOn="{StaticResource {x:Static GridView.GridViewItemContainerStyleKey}}">
    <Setter Property="b:ThemeStyleBehavior.AutoMergeStyle" Value="True" />
  </Style>
  <Style x:Key="{x:Static l:Resources.TreeViewItemExpanderStyleKey}" TargetType="{x:Type ToggleButton}" BasedOn="{StaticResource {x:Type ToggleButton}}">
    <Setter Property="b:ThemeStyleBehavior.AutoMergeStyle" Value="True" />
    <Setter Property="Focusable" Value="False" />
    <Setter Property="Width" Value="16" />
    <Setter Property="Height" Value="16" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ToggleButton}">
          <Border Width="16" Height="16" Background="#00FFFFFF" Padding="5,5,5,5">
            <Path Name="ExpandPath" Fill="#00FFFFFF" Stroke="#FF989898" Data="M0,0 L0,6 L6,0">
              <UIElement.RenderTransform>
                <RotateTransform Angle="135" CenterX="3" CenterY="3" />
              </UIElement.RenderTransform>
            </Path>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="UIElement.IsMouseOver" Value="True">
              <Setter TargetName="ExpandPath" Property="Shape.Stroke" Value="#FF1BBBFA" />
              <Setter TargetName="ExpandPath" Property="Shape.Fill" Value="#00FFFFFF" />
            </Trigger>
            <Trigger Property="ToggleButton.IsChecked" Value="True">
              <Setter TargetName="ExpandPath" Property="RenderTransform">
                <Setter.Value>
                  <RotateTransform Angle="180" CenterX="3" CenterY="3" />
                </Setter.Value>
              </Setter>
              <Setter TargetName="ExpandPath" Property="Shape.Fill" Value="#FF595959" />
              <Setter TargetName="ExpandPath" Property="Shape.Stroke" Value="#FF262626" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{x:Static l:Resources.TreeViewIconStyleKey}" TargetType="{x:Type ct:Icon}" BasedOn="{StaticResource {x:Static l:Resources.AutoGreyStyleKey}}">
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="Margin" Value="0,2,4,2" />
    <Setter Property="Height" Value="14" />
    <Setter Property="Width" Value="14" />
    <Style.Triggers>
      <MultiTrigger>
        <MultiTrigger.Conditions>
          <Condition Property="Source" Value="{x:Null}" />
          <Condition Property="ct:Icon.SourceBrush" Value="{x:Null}" />
        </MultiTrigger.Conditions>
        <Setter Property="Visibility" Value="Collapsed" />
      </MultiTrigger>
    </Style.Triggers>
  </Style>
  <Style x:Key="{x:Static l:Resources.TreeViewLabelTextBlockStyleKey}" TargetType="{x:Type TextBlock}" BasedOn="{StaticResource {x:Type TextBlock}}">
    <Setter Property="b:ThemeStyleBehavior.AutoMergeStyle" Value="True" />
    <Setter Property="VerticalAlignment" Value="Center" />
    <Setter Property="FontWeight" Value="{Binding FontWeight}" />
    <Setter Property="FontStyle" Value="{Binding FontItalicStyle}" />
    <Setter Property="b:EditableTextBlockBehavior.IsInEditMode" Value="{Binding IsInLabelEditMode, Mode=TwoWay}" />
  </Style>
  <Style x:Key="{x:Static l:Resources.TreeViewItemStyleKey}" TargetType="{x:Type TreeViewItem}" BasedOn="{StaticResource {x:Type TreeViewItem}}">
    <Style.Triggers>
      <Trigger Property="VirtualizingPanel.IsVirtualizing" Value="True">
        <Setter Property="ItemsPanel">
          <Setter.Value>
            <ItemsPanelTemplate>
              <VirtualizingStackPanel />
            </ItemsPanelTemplate>
          </Setter.Value>
        </Setter>
      </Trigger>
    </Style.Triggers>
    <Setter Property="b:ThemeStyleBehavior.AutoMergeStyle" Value="True" />
    <Setter Property="TreeViewItem.IsExpanded" Value="{Binding Expanded, Mode=TwoWay}" />
    <Setter Property="IsEnabled" Value="{Binding IsEnabled, Mode=OneWay}" />
    <Setter Property="Visibility" Value="{Binding IsVisible, Mode=OneWay, Converter={c:BoolToVisibilityConverter}}" />
    <Setter Property="b:AtfTreeViewBehavior.IsMultiSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
    <Setter Property="b:AtfTreeViewBehavior.IsChecked" Value="{Binding CheckState, Mode=TwoWay}" />
    <Setter Property="b:StyleBehaviors.Behaviors">
      <Setter.Value>
        <b:StyleBehaviorCollection>
          <b:TreeViewItemLabelEditBehavior />
        </b:StyleBehaviorCollection>
      </Setter.Value>
    </Setter>
    <Setter Property="Background" Value="#00FFFFFF" />
    <Setter Property="Padding" Value="1,0,0,0" />
    <Setter Property="FrameworkElement.FocusVisualStyle">
      <Setter.Value>
        <Style TargetType="{x:Type IFrameworkInputElement}">
          <Setter Property="Control.Template">
            <Setter.Value>
              <ControlTemplate>
                <Rectangle />
              </ControlTemplate>
            </Setter.Value>
          </Setter>
        </Style>
      </Setter.Value>
    </Setter>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type TreeViewItem}">
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition MinWidth="16" Width="Auto" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition />
            </Grid.RowDefinitions>
            <ToggleButton Name="Expander" Style="{DynamicResource {x:Static l:Resources.TreeViewItemExpanderStyleKey}}" ClickMode="Press" IsChecked="{Binding Path=IsExpanded, RelativeSource={RelativeSource TemplatedParent}}" />
            <Border Name="Bd" Grid.Column="1" Background="{TemplateBinding Control.Background}" BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="{TemplateBinding Control.BorderThickness}" Padding="{TemplateBinding Control.Padding}" SnapsToDevicePixels="True">
              <ContentPresenter Name="PART_Header" ContentSource="Header" HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}" SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" />
            </Border>
            <ItemsPresenter Name="ItemsHost" Grid.Row="1" Grid.Column="1" />
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="TreeViewItem.IsExpanded" Value="False">
              <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="ItemsControl.HasItems" Value="False">
              <Setter TargetName="Expander" Property="Visibility" Value="Hidden" />
            </Trigger>
            <Trigger Property="b:AtfTreeViewBehavior.IsMultiSelected" Value="True">
              <Setter TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" Property="Border.Background" />
              <Setter Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}" Property="Foreground" />
            </Trigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="b:AtfTreeViewBehavior.IsMultiSelected" Value="True" />
                <Condition Property="TreeViewItem.IsSelectionActive" Value="False" />
              </MultiTrigger.Conditions>
              <Setter TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" Property="Border.Background" />
              <Setter Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" Property="Foreground" />
            </MultiTrigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" Property="Foreground" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{x:Type ToolTip}" TargetType="{x:Type ToolTip}">
    <Style.Triggers>
      <Trigger Property="Content" Value="{x:Static sys:String.Empty}">
        <Setter Property="Visibility" Value="Collapsed" />
      </Trigger>
      <Trigger Property="Content" Value="{x:Null}">
        <Setter Property="Visibility" Value="Collapsed" />
      </Trigger>
    </Style.Triggers>
  </Style>
  <HierarchicalDataTemplate x:Key="{x:Static l:Resources.DefaultTreeViewItemTemplateKey}" ItemsSource="{Binding Children}">
    <StackPanel Orientation="Horizontal" ToolTipService.ToolTip="{Binding HoverText}">
      <CheckBox VerticalAlignment="Center" Margin="0,0,2,0" Focusable="False" Visibility="{Binding HasCheck, Converter={c:BoolToVisibilityConverter}}" IsChecked="{Binding Path=(b:AtfTreeViewBehavior.IsChecked), Mode=TwoWay, RelativeSource={RelativeSource AncestorType={x:Type TreeViewItem}}}" />
      <ct:Icon Margin="0,2,2,2" Width="8" Height="8" Style="{StaticResource {x:Static l:Resources.TreeViewIconStyleKey}}" SourceKey="{Binding StateImageKey}" RedChroma="{Binding Path=(ct:Icon.RedChroma), RelativeSource={RelativeSource AncestorType={x:Type TreeViewItem}}}" UseShadow="{Binding Path=(ct:Icon.UseShadow), RelativeSource={RelativeSource AncestorType={x:Type TreeViewItem}}}" />
      <ct:Icon Style="{StaticResource {x:Static l:Resources.TreeViewIconStyleKey}}" SourceKey="{Binding ImageKey}" RedChroma="{Binding Path=(ct:Icon.RedChroma), RelativeSource={RelativeSource AncestorType={x:Type TreeViewItem}}}" UseShadow="{Binding Path=(ct:Icon.UseShadow), RelativeSource={RelativeSource AncestorType={x:Type TreeViewItem}}}" />
      <TextBlock Style="{DynamicResource {x:Static l:Resources.TreeViewLabelTextBlockStyleKey}}" Text="{Binding Label, Mode=TwoWay}" />
    </StackPanel>
  </HierarchicalDataTemplate>
  <Style x:Key="{x:Static l:Resources.TreeViewStyleKey}" TargetType="{x:Type TreeView}" BasedOn="{StaticResource {x:Type TreeView}}">
    <Setter Property="b:ThemeStyleBehavior.AutoMergeStyle" Value="True" />
    <Setter Property="ItemContainerStyle" Value="{DynamicResource {x:Static l:Resources.TreeViewItemStyleKey}}" />
    <Setter Property="ItemsSource" Value="{Binding Roots}" />
    <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True" />
    <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
    <Setter Property="ItemTemplate" Value="{DynamicResource {x:Static l:Resources.DefaultTreeViewItemTemplateKey}}" />
    <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Hidden" />
    <Setter Property="b:StyleBehaviors.Behaviors">
      <Setter.Value>
        <b:StyleBehaviorCollection>
          <b:AtfTreeViewBehavior />
          <b:ItemsControlContextMenuBehavior />
          <b:SelectionContextDragSourceBehavior />
          <b:ItemsControlDropTargetBehavior />
          <b:DragAutoScrollBehavior />
          <b:TreeViewDragHoverExpandBehavior />
        </b:StyleBehaviorCollection>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{x:Static l:Resources.TreeListViewStyleKey}" TargetType="{x:Type ct:TreeListView}" BasedOn="{StaticResource {x:Type ct:TreeListView}}">
    <Setter Property="b:ThemeStyleBehavior.AutoMergeStyle" Value="True" />
    <Setter Property="ItemsSource" Value="{Binding Roots}" />
    <Setter Property="ItemContainerStyle" Value="{DynamicResource {x:Static l:Resources.TreeListViewItemStyleKey}}" />
    <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True" />
    <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
    <Setter Property="ItemTemplate" Value="{DynamicResource {x:Static l:Resources.DefaultTreeViewItemTemplateKey}}" />
    <Setter Property="b:StyleBehaviors.Behaviors">
      <Setter.Value>
        <b:StyleBehaviorCollection>
          <b:AtfTreeViewBehavior />
          <b:ItemsControlContextMenuBehavior />
          <b:SelectionContextDragSourceBehavior />
          <b:ItemsControlDropTargetBehavior />
          <b:DragAutoScrollBehavior />
          <b:TreeViewDragHoverExpandBehavior />
        </b:StyleBehaviorCollection>
      </Setter.Value>
    </Setter>
  </Style>
  <Style x:Key="{x:Static l:Resources.TreeListViewItemStyleKey}" TargetType="{x:Type ct:TreeListViewItem}" BasedOn="{StaticResource {x:Type ct:TreeListViewItem}}">
    <Setter Property="b:ThemeStyleBehavior.AutoMergeStyle" Value="True" />
    <Setter Property="TreeViewItem.IsExpanded" Value="{Binding Expanded, Mode=TwoWay}" />
    <Setter Property="b:AtfTreeViewBehavior.IsMultiSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
    <Setter Property="b:StyleBehaviors.Behaviors">
      <Setter.Value>
        <b:StyleBehaviorCollection>
          <b:TreeViewItemLabelEditBehavior />
        </b:StyleBehaviorCollection>
      </Setter.Value>
    </Setter>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ct:TreeListViewItem}">
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="*" />
              <RowDefinition />
            </Grid.RowDefinitions>
            <Border Name="Bd" Background="{TemplateBinding Control.Background}" BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="{TemplateBinding Control.BorderThickness}" Padding="{TemplateBinding Control.Padding}">
              <ct:TreeGridViewRowPresenter x:Name="PART_Header" Content="{TemplateBinding HeaderedItemsControl.Header}" FirstColumnIndent="{Binding Level, ConverterParameter=19, Converter={ct:LevelToIndentConverter}, RelativeSource={RelativeSource AncestorType={x:Type ct:TreeListViewItem}}}" Columns="{Binding Path=Columns, RelativeSource={RelativeSource AncestorType={x:Type ct:TreeListView}}}">
                <ct:TreeGridViewRowPresenter.Expander>
                  <Grid>
                    <ToggleButton Name="Expander" Style="{DynamicResource {x:Static l:Resources.TreeViewItemExpanderStyleKey}}" ClickMode="Press" IsChecked="{Binding Path=IsExpanded, RelativeSource={RelativeSource TemplatedParent}}" />
                  </Grid>
                </ct:TreeGridViewRowPresenter.Expander>
              </ct:TreeGridViewRowPresenter>
            </Border>
            <ItemsPresenter Name="ItemsHost" Grid.Row="1" />
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="TreeViewItem.IsExpanded" Value="False">
              <Setter TargetName="ItemsHost" Property="Visibility" Value="Collapsed" />
            </Trigger>
            <Trigger Property="ItemsControl.HasItems" Value="False">
              <Setter TargetName="Expander" Property="Visibility" Value="Hidden" />
            </Trigger>
            <Trigger Property="b:AtfTreeViewBehavior.IsMultiSelected" Value="True">
              <Setter TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.HighlightBrushKey}}" Property="Border.Background" />
              <Setter Value="{DynamicResource {x:Static SystemColors.HighlightTextBrushKey}}" Property="Foreground" />
            </Trigger>
            <Trigger Property="b:AtfTreeViewBehavior.IsMultiSelected" Value="False">
              <Setter Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" Property="Foreground" />
            </Trigger>
            <MultiTrigger>
              <MultiTrigger.Conditions>
                <Condition Property="b:AtfTreeViewBehavior.IsMultiSelected" Value="True" />
                <Condition Property="TreeViewItem.IsSelectionActive" Value="False" />
              </MultiTrigger.Conditions>
              <Setter TargetName="Bd" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" Property="Border.Background" />
              <Setter Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" Property="Foreground" />
            </MultiTrigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}" Property="Foreground" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>
  <DataTemplate x:Key="{DataTemplateKey {x:Type m:NodeTypePaletteItem}}" DataType="{x:Type m:NodeTypePaletteItem}">
    <StackPanel Orientation="Horizontal">
      <ct:Icon Margin="0,2,2,2" Width="16" Height="16" RedChroma="{DynamicResource ItemIconBrush}" SourceKey="{Binding ImageKey}" />
      <TextBlock Margin="2" Text="{Binding Name}" />
      <ToolTipService.ToolTip>
        <ContentControl Content="{Binding Description}" />
      </ToolTipService.ToolTip>
    </StackPanel>
  </DataTemplate>
  <DataTemplate x:Key="{DataTemplateKey {x:Type m:PaletteContent}}" DataType="{x:Type m:PaletteContent}">
    <ListBox AllowDrop="False" HorizontalContentAlignment="Stretch" ScrollViewer.CanContentScroll="False" ItemsSource="{Binding Data}">
      <ItemsControl.GroupStyle>
        <GroupStyle>
          <GroupStyle.ContainerStyle>
            <Style TargetType="{x:Type GroupItem}">
              <Setter Property="Template">
                <Setter.Value>
                  <ControlTemplate TargetType="{x:Type GroupItem}">
                    <Expander IsExpanded="True" Margin="1">
                      <HeaderedContentControl.Header>
                        <TextBlock FontWeight="Bold" Text="{Binding Path=Name}" />
                      </HeaderedContentControl.Header>
                      <ItemsPresenter Margin="5,0,0,0" />
                    </Expander>
                  </ControlTemplate>
                </Setter.Value>
              </Setter>
            </Style>
          </GroupStyle.ContainerStyle>
        </GroupStyle>
      </ItemsControl.GroupStyle>
      <i:Interaction.Behaviors>
        <b:ItemsControlDragSourceBehavior />
      </i:Interaction.Behaviors>
    </ListBox>
  </DataTemplate>
  <Style x:Key="{x:Static l:Resources.RadioButtonListStyleKey}" TargetType="{x:Type ListBox}">
    <Setter Property="FrameworkElement.OverridesDefaultStyle" Value="True" />
    <Setter Property="UIElement.SnapsToDevicePixels" Value="True" />
    <Setter Property="BorderThickness" Value="0" />
    <Setter Property="Padding" Value="0" />
    <Setter Property="Margin" Value="0" />
    <Setter Property="Background" Value="#00FFFFFF" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ListBox}">
          <Border SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" BorderBrush="{TemplateBinding Control.BorderBrush}" Background="{TemplateBinding Control.Background}" BorderThickness="{TemplateBinding Control.BorderThickness}" Padding="{TemplateBinding Control.Padding}">
            <StackPanel IsItemsHost="True" />
          </Border>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
    <Setter Property="ItemContainerStyle">
      <Setter.Value>
        <Style TargetType="{x:Type ListBoxItem}">
          <Setter Property="Padding" Value="0" />
          <Setter Property="FrameworkElement.FocusVisualStyle" Value="{x:Null}" />
          <Setter Property="Template">
            <Setter.Value>
              <ControlTemplate TargetType="{x:Type ListBoxItem}">
                <Border Background="#00FFFFFF" BorderThickness="{TemplateBinding Control.BorderThickness}" Padding="{TemplateBinding Control.Padding}" BorderBrush="{TemplateBinding Control.BorderBrush}" SnapsToDevicePixels="True">
                  <RadioButton Focusable="False" IsHitTestVisible="False" IsChecked="{TemplateBinding ListBoxItem.IsSelected}">
                    <ContentPresenter />
                  </RadioButton>
                </Border>
              </ControlTemplate>
            </Setter.Value>
          </Setter>
        </Style>
      </Setter.Value>
    </Setter>
  </Style>
</ResourceDictionary>