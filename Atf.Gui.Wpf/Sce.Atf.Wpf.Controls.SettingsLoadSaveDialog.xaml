<?xml version="1.0" encoding="utf-8"?>
<c:CommonDialog x:Class="Sce.Atf.Wpf.Controls.SettingsLoadSaveDialog" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:l="clr-namespace:Sce.Atf.Wpf" xmlns:m="clr-namespace:Sce.Atf.Wpf.Markup" xmlns:c="clr-namespace:Sce.Atf.Wpf.Controls" xmlns:vc="clr-namespace:Sce.Atf.Wpf.ValueConverters" ResizeMode="NoResize" ShowInTaskbar="False" SizeToContent="WidthAndHeight">
  <DockPanel LastChildFill="True">
    <DockPanel Dock="Bottom" LastChildFill="False" Margin="0,12,0,0">
      <Button DockPanel.Dock="Right" Style="{StaticResource {x:Static l:Resources.DialogButtonStyleKey}}" Command="{Binding CancelCommand}" Content="{m:Loc Cancel}" />
      <Button DockPanel.Dock="Right" Margin="0" IsDefault="True" Style="{StaticResource {x:Static l:Resources.DialogButtonStyleKey}}" Command="{Binding OkCommand}" Content="{m:Loc Proceed}" />
    </DockPanel>
    <ListBox DockPanel.Dock="Top" Style="{StaticResource {x:Static l:Resources.RadioButtonListStyleKey}}" Margin="16,8,16,0" SelectedValuePath="Content" SelectedValue="{Binding Action}">
      <FrameworkElement.Resources>
        <ResourceDictionary>
          <DataTemplate x:Key="{DataTemplateKey {x:Type c:SettingsAction}}" DataType="{x:Type c:SettingsAction}">
            <StackPanel>
              <TextBlock Text="{Binding Converter={vc:DisplayStringConverter}}" />
              <TextBlock TextWrapping="Wrap" Margin="0,5,10,10" Text="{Binding Converter={vc:EnumDescriptionConverter}}" />
            </StackPanel>
          </DataTemplate>
        </ResourceDictionary>
      </FrameworkElement.Resources>
      <ListBoxItem Content="{x:Static c:SettingsAction.Save}" />
      <ListBoxItem Content="{x:Static c:SettingsAction.Load}" />
    </ListBox>
  </DockPanel>
</c:CommonDialog>