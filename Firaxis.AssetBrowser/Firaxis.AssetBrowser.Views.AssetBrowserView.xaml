<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="Firaxis.AssetBrowser.Views.AssetBrowserView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:abvm="clr-namespace:Firaxis.AssetBrowser.ViewModels" xmlns:mvvma="clr-namespace:Firaxis.MVVMBase.Attached;assembly=Firaxis.MVVMBase" xmlns:mvvmc="clr-namespace:Firaxis.MVVMBase.Controls;assembly=Firaxis.MVVMBase" xmlns:aba="clr-namespace:Firaxis.AssetBrowser.Attached" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" Margin="5" aba:SkinServiceHelper.ApplySkin="True" xmlns:views="clr-namespace:Firaxis.AssetBrowser.Views">
  <FrameworkElement.Resources>
    <ResourceDictionary>
      <Style x:Key="gridViewListBoxItem" BasedOn="{StaticResource boldListboxSelection}" TargetType="{x:Type ListBoxItem}">
        <Setter Property="Background" Value="{Binding Converter={StaticResource BackgroundConverter}, RelativeSource={RelativeSource Self}}" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type ListViewItem}">
              <Border BorderBrush="#00FFFFFF" BorderThickness="0" Background="{TemplateBinding Control.Background}">
                <GridViewRowPresenter HorizontalAlignment="Stretch" VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}" Width="Auto" Margin="0" Content="{TemplateBinding ContentControl.Content}" />
              </Border>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
      </Style>
      <Style x:Key="stMainListView" TargetType="{x:Type ListView}">
        <Setter Property="ListBox.SelectionMode" Value="Extended" />
        <Style.Triggers>
          <DataTrigger Value="{x:Static abvm:ViewMode.ExtraLargeIcons}" Binding="{Binding SelectedViewMode}">
            <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
            <Setter Property="ItemContainerStyle" Value="{StaticResource boldListboxSelection}" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="ItemsPanel">
              <Setter.Value>
                <ItemsPanelTemplate>
                  <mvvmc:VirtualizingWrapPanel WrapOrientation="Horizontal" ChildWidth="270" ChildHeight="290" />
                </ItemsPanelTemplate>
              </Setter.Value>
            </Setter>
            <Setter Property="ItemTemplate">
              <Setter.Value>
                <DataTemplate>
                  <StackPanel Orientation="Vertical" Margin="1" Width="270" Height="290" VerticalAlignment="Top" HorizontalAlignment="Center" ToolTip="{Binding ToolTip}">
                    <Image aba:EntityThumbnailCache.Size="{x:Static abvm:ViewMode.ExtraLargeIcons}" VerticalAlignment="Top" HorizontalAlignment="Center" Width="256" Height="256" aba:EntityThumbnailCache.Entity="{Binding}" />
                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Top" TextWrapping="Wrap" Width="256" Text="{Binding Name}" />
                  </StackPanel>
                </DataTemplate>
              </Setter.Value>
            </Setter>
          </DataTrigger>
          <DataTrigger Value="{x:Static abvm:ViewMode.ClassicDetails}" Binding="{Binding SelectedViewMode}">
            <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Standard" />
            <Setter Property="mvvma:ListViewHelper.SortOnColumnHeaderClick" Value="True" />
            <Setter Property="ItemContainerStyle" Value="{StaticResource gridViewListBoxItem}" />
            <Setter Property="ItemsPanel">
              <Setter.Value>
                <ItemsPanelTemplate>
                  <VirtualizingStackPanel />
                </ItemsPanelTemplate>
              </Setter.Value>
            </Setter>
            <Setter Property="ListView.View">
              <Setter.Value>
                <GridView>
                  <GridView.ColumnHeaderContainerStyle>
                    <Style TargetType="{x:Type GridViewColumnHeader}">
                      <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" />
                      <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
                    </Style>
                  </GridView.ColumnHeaderContainerStyle>
                  <GridViewColumn Width="50">
                    <GridViewColumn.CellTemplate>
                      <DataTemplate>
                        <Image aba:EntityThumbnailCache.Size="{x:Static abvm:ViewMode.ClassicDetails}" VerticalAlignment="Center" HorizontalAlignment="Center" Width="32" Height="32" aba:EntityThumbnailCache.Entity="{Binding}" />
                      </DataTemplate>
                    </GridViewColumn.CellTemplate>
                  </GridViewColumn>
                  <GridViewColumn Width="200" DisplayMemberBinding="{Binding Name}">
                    <GridViewColumnHeader Content="Name" mvvma:ListViewHelper.SortPropertyName="Name" mvvma:ListViewHelper.InitialSort="True" />
                    <GridViewColumn.CellTemplate>
                      <DataTemplate>
                        <TextBlock Name="nameText" Foreground="#FFFFFFFF" Text="{Binding Name}" />
                      </DataTemplate>
                    </GridViewColumn.CellTemplate>
                  </GridViewColumn>
                  <GridViewColumn Width="140" DisplayMemberBinding="{Binding PerforceStatus}">
                    <GridViewColumnHeader mvvma:ListViewHelper.SortPropertyName="PerforceStatus" Content="Status" />
                  </GridViewColumn>
                  <GridViewColumn Width="140" DisplayMemberBinding="{Binding PerforceModTime}">
                    <GridViewColumnHeader mvvma:ListViewHelper.SortPropertyName="PerforceModTime" Content="Last Modified" />
                  </GridViewColumn>
                  <GridViewColumn Width="50" TextBlock.Foreground="#FFFFFFFF" DisplayMemberBinding="{Binding Project}">
                    <GridViewColumnHeader mvvma:ListViewHelper.SortPropertyName="Project" Content="Project" />
                  </GridViewColumn>
                </GridView>
              </Setter.Value>
            </Setter>
          </DataTrigger>
          <DataTrigger Value="{x:Static abvm:ViewMode.LargeIcons}" Binding="{Binding SelectedViewMode}">
            <Setter Property="ItemContainerStyle" Value="{StaticResource boldListboxSelection}" />
            <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="ItemsPanel">
              <Setter.Value>
                <ItemsPanelTemplate>
                  <mvvmc:VirtualizingWrapPanel WrapOrientation="Horizontal" ChildWidth="104" ChildHeight="148" />
                </ItemsPanelTemplate>
              </Setter.Value>
            </Setter>
            <Setter Property="ItemTemplate">
              <Setter.Value>
                <DataTemplate>
                  <StackPanel Orientation="Vertical" Margin="1" Width="104" Height="148" VerticalAlignment="Top" HorizontalAlignment="Center" ToolTip="{Binding ToolTip}">
                    <Image aba:EntityThumbnailCache.Size="{x:Static abvm:ViewMode.LargeIcons}" VerticalAlignment="Top" HorizontalAlignment="Center" Width="96" Height="96" aba:EntityThumbnailCache.Entity="{Binding}" />
                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Top" TextWrapping="Wrap" Width="96" Text="{Binding Name}" />
                  </StackPanel>
                </DataTemplate>
              </Setter.Value>
            </Setter>
          </DataTrigger>
          <DataTrigger Value="{x:Static abvm:ViewMode.List}" Binding="{Binding SelectedViewMode}">
            <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
            <Setter Property="ItemContainerStyle" Value="{StaticResource boldListboxSelection}" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="ItemsPanel">
              <Setter.Value>
                <ItemsPanelTemplate>
                  <mvvmc:VirtualizingWrapPanel WrapOrientation="Horizontal" ChildWidth="500" ChildHeight="24" />
                </ItemsPanelTemplate>
              </Setter.Value>
            </Setter>
            <Setter Property="Grid.IsSharedSizeScope" Value="True" />
            <Setter Property="ItemTemplate">
              <Setter.Value>
                <DataTemplate>
                  <Grid Margin="2 1 0 1" Height="18" HorizontalAlignment="Left" ToolTip="{Binding ToolTip}">
                    <Grid.ColumnDefinitions>
                      <ColumnDefinition Width="Auto" />
                      <ColumnDefinition Width="*" SharedSizeGroup="EntityName" />
                    </Grid.ColumnDefinitions>
                    <Image aba:EntityThumbnailCache.Size="{x:Static abvm:ViewMode.List}" VerticalAlignment="Center" HorizontalAlignment="Center" Width="16" Height="16" Grid.Column="0" aba:EntityThumbnailCache.Entity="{Binding}" />
                    <TextBlock HorizontalAlignment="Left" VerticalAlignment="Center" Margin="4,0,0,0" Grid.Column="1" Text="{Binding Name}" />
                  </Grid>
                </DataTemplate>
              </Setter.Value>
            </Setter>
          </DataTrigger>
          <DataTrigger Value="{x:Static abvm:ViewMode.MediumIcons}" Binding="{Binding SelectedViewMode}">
            <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="ItemsPanel">
              <Setter.Value>
                <ItemsPanelTemplate>
                  <mvvmc:VirtualizingWrapPanel WrapOrientation="Horizontal" ChildWidth="64" ChildHeight="128" />
                </ItemsPanelTemplate>
              </Setter.Value>
            </Setter>
            <Setter Property="ItemTemplate">
              <Setter.Value>
                <DataTemplate>
                  <StackPanel Orientation="Vertical" Margin="1" Width="64" Height="128" VerticalAlignment="Top" HorizontalAlignment="Center" ToolTip="{Binding ToolTip}">
                    <Image aba:EntityThumbnailCache.Size="{x:Static abvm:ViewMode.MediumIcons}" VerticalAlignment="Top" HorizontalAlignment="Center" Width="48" Height="48" aba:EntityThumbnailCache.Entity="{Binding}" />
                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Top" TextWrapping="Wrap" Width="56" Text="{Binding Name}" />
                  </StackPanel>
                </DataTemplate>
              </Setter.Value>
            </Setter>
          </DataTrigger>
          <DataTrigger Value="{x:Static abvm:ViewMode.Tiles}" Binding="{Binding SelectedViewMode}">
            <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
            <Setter Property="ItemsPanel">
              <Setter.Value>
                <ItemsPanelTemplate>
                  <mvvmc:VirtualizingWrapPanel WrapOrientation="Horizontal" ChildWidth="256" ChildHeight="48" />
                </ItemsPanelTemplate>
              </Setter.Value>
            </Setter>
            <Setter Property="ItemTemplate">
              <Setter.Value>
                <DataTemplate>
                  <StackPanel Orientation="Horizontal" Margin="1" Width="256" VerticalAlignment="Top" ToolTip="{Binding ToolTip}">
                    <Image aba:EntityThumbnailCache.Size="{x:Static abvm:ViewMode.Tiles}" DockPanel.Dock="Left" VerticalAlignment="Center" HorizontalAlignment="Center" Width="48" Height="48" aba:EntityThumbnailCache.Entity="{Binding}" />
                    <StackPanel Orientation="Vertical" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="4 0 0 0">
                      <TextBlock Text="{Binding Name}" />
                      <TextBlock Text="{Binding Project}" />
                      <TextBlock Text="{Binding PerforceStatus}" />
                    </StackPanel>
                  </StackPanel>
                </DataTemplate>
              </Setter.Value>
            </Setter>
          </DataTrigger>
          <DataTrigger Value="False" Binding="{Binding AllowMultipleSelection}">
            <Setter Property="ListBox.SelectionMode" Value="Single" />
          </DataTrigger>
        </Style.Triggers>
      </Style>
      <Style x:Key="{x:Type ComboBox}" TargetType="{x:Type ComboBox}">
        <Setter Property="Foreground" Value="#FF000000" />
        <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlLightLightBrushKey}}" />
      </Style>
      <Style x:Key="{x:Type ComboBoxItem}" TargetType="{x:Type ComboBoxItem}">
        <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
      </Style>
      <Style x:Key="{x:Static ToolBar.ButtonStyleKey}" TargetType="{x:Type Button}">
        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
        <Setter Property="Padding" Value="2" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Background" Value="#00FFFFFF" />
        <Setter Property="BorderBrush" Value="#00FFFFFF" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="HorizontalContentAlignment" Value="Center" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
          <Setter.Value>
            <ControlTemplate TargetType="{x:Type Button}">
              <Border Name="Bd" SnapsToDevicePixels="True" Background="{TemplateBinding Control.Background}" BorderBrush="{TemplateBinding Control.BorderBrush}" BorderThickness="{TemplateBinding Control.BorderThickness}" Padding="{TemplateBinding Control.Padding}">
                <ContentPresenter HorizontalAlignment="{TemplateBinding Control.HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding Control.VerticalContentAlignment}" SnapsToDevicePixels="{TemplateBinding UIElement.SnapsToDevicePixels}" />
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="UIElement.IsMouseOver" Value="True">
                  <Setter TargetName="Bd" Property="Border.BorderBrush" Value="#FFF8F8FF" />
                </Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate>
          </Setter.Value>
        </Setter>
        <Style.Resources>
          <ResourceDictionary>
            <Style x:Key="{x:Type Image}" TargetType="{x:Type Image}">
              <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                  <Setter Property="UIElement.Opacity" Value="0.25" />
                </Trigger>
              </Style.Triggers>
            </Style>
          </ResourceDictionary>
        </Style.Resources>
      </Style>
    </ResourceDictionary>
  </FrameworkElement.Resources>
  <FrameworkElement.Style>
    <Style>
      <Style.Triggers>
        <DataTrigger Value="True" Binding="{Binding StartFocused}">
          <Setter Property="mvvma:FocusHelper.FocusElementNameOnLoad" Value="searchTextBox" />
        </DataTrigger>
      </Style.Triggers>
    </Style>
  </FrameworkElement.Style>
  <DockPanel Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" Margin="5">
    <DockPanel Name="searchDock" Dock="Top">
      <TextBlock Name="searchLabel" Text="Search: " VerticalAlignment="Center" Margin="0 0 5 0" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" />
      <Button Name="ClearSearchButton" Content="X" Width="24" Height="24" Foreground="#FF000000" DockPanel.Dock="Right" ToolTip="Clear the search bar." Command="{Binding ClearSearchCommand}" />
      <Border Background="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}" DockPanel.Dock="Right" Margin="3" BorderBrush="{DynamicResource {x:Static SystemColors.ControlLightBrushKey}}" BorderThickness="3">
        <TextBox Name="searchTextBox" ToolTip="Search text. To use an 'OR' search, use the '|' character." Background="{DynamicResource {x:Static SystemColors.ControlDarkDarkBrushKey}}" mvvma:TextBoxHelper.SelectAllOnFocus="True" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}">
          <mvvma:FocusHelper.FocusNavigationBindings>
            <mvvma:FocusNavigationBindingCollection>
              <mvvma:FocusNavigationBinding Key="Down" ElementName="filteredEntitiesListView" CanNavigate="{Binding IsShowingEntitiesFunc}" />
            </mvvma:FocusNavigationBindingCollection>
          </mvvma:FocusHelper.FocusNavigationBindings>
        </TextBox>
      </Border>
    </DockPanel>
    <mvvmc:AutoAdjustingStackPanel Name="mainGrid" DockPanel.Dock="Top">
      <ContentControl Name="FilterView" mvvmc:AutoAdjustingStackPanel.ChildWidth="1.2*" mvvmc:AutoAdjustingStackPanel.ChildHeight="1*" Content="{Binding FilterVM}" />
      <mvvmc:AutoAdjustingStackPanelSplitter />
      <DockPanel Name="assetGrid" mvvmc:AutoAdjustingStackPanel.ChildWidth="4*" mvvmc:AutoAdjustingStackPanel.ChildHeight="4*">
        <DockPanel Name="browserToolbar" Margin="0 1 0 2" VerticalAlignment="Center" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" Dock="Top">
          <ComboBox VerticalAlignment="Center" DockPanel.Dock="Right" ItemsSource="{Binding ViewModes}" SelectedItem="{Binding SelectedViewMode}" />
          <ItemsControl Name="EntityCommands" Margin="4 0 5 0" VerticalAlignment="Center" DockPanel.Dock="Left" Visibility="{Binding HasCommands, Converter={StaticResource BoolToVis}}" ItemsSource="{Binding EntityCommands}">
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate>
                <StackPanel Orientation="Horizontal" />
              </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Button ToolTipService.ShowOnDisabled="True" Margin="2 0 2 0" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" Background="{DynamicResource {x:Static SystemColors.ControlDarkBrushKey}}" ToolTip="{Binding Name}" Command="{Binding Command}" CommandParameter="{Binding ElementName=filteredEntitiesListView, Path=SelectedItems}">
                  <Image Width="16" Height="16" Source="{Binding Thumbnail}" ToolTip="{Binding Name}" />
                </Button>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </DockPanel>
        <ListView Name="filteredEntitiesListView" Style="{StaticResource stMainListView}" Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" OverridesDefaultStyle="False" VirtualizingPanel.IsVirtualizingWhenGrouping="True" VirtualizingPanel.IsVirtualizing="True" ScrollViewer.CanContentScroll="True" mvvma:ListBoxHelper.UnselectOnClickEmpty="True" mvvma:ListBoxHelper.PassFocusToSelectedIndex="True" aba:SkinServiceHelper.ApplySkin="True" ItemsSource="{Binding FilteredEntities, IsAsync=True}" mvvma:ListBoxHelper.SelectionChangedAction="{Binding EntitySelectionChangedAction}" mvvma:DragDropHelper.DragDataObjectFactory="{Binding CreateDragDataObjectFunc}">
          <UIElement.InputBindings>
            <KeyBinding Key="Enter" Command="{Binding OpenSelectedEntitiesCommand}" />
          </UIElement.InputBindings>
          <mvvma:MouseHelper.CommandCollection>
            <mvvma:MouseCommandCollection>
              <mvvma:MouseCommand Clicks="2" InputStyle="Click" Command="{Binding OpenSelectedEntitiesCommand}" />
            </mvvma:MouseCommandCollection>
          </mvvma:MouseHelper.CommandCollection>
        </ListView>
      </DockPanel>
      <mvvmc:AutoAdjustingStackPanelSplitter />
      <DataGrid Name="selectedEntityDetails" AutoGenerateColumns="False" HeadersVisibility="None" GridLinesVisibility="None" IsReadOnly="True" BorderThickness="1" MinHeight="75" mvvmc:AutoAdjustingStackPanel.ChildWidth="2*" mvvmc:AutoAdjustingStackPanel.ChildHeight="2*" ItemsSource="{Binding SelectedEntityInfo, Mode=OneWay}">
        <DataGrid.Columns>
          <DataGridTextColumn Binding="{Binding label}" />
          <DataGridTextColumn Binding="{Binding value}" />
        </DataGrid.Columns>
      </DataGrid>
    </mvvmc:AutoAdjustingStackPanel>
  </DockPanel>
</UserControl>